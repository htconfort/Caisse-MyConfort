{
  "name": "turbo-mode-myconfort",
  "displayName": "Turbo Mode MyConfort",
  "description": "Active la configuration optimisée pour React + Vite + TypeScript",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "turboMode.activate",
        "title": "Activer Turbo Mode MyConfort"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.4"
  }
}
