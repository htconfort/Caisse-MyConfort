# Configuration npm pour le monorepo Caisse MyConfort
# Assure la compatibilité avec Netlify et les builds locaux

# Utilise npm version 10.x
engine-strict=false

# Cache des modules
cache=/tmp/npm-cache

# Pas de package-lock à la racine (le lock est dans mon-projet-vite)
package-lock=false

# Installation rapide
prefer-offline=true

